import 'mocha';
import 'reflect-metadata';
import { expect } from 'chai';

describe('tests', () => {
	describe('endpoint', () => {
		it('should pass', () => {
			expect(true).to.equal(true);
		});
	});
});